/// @description Load 
obj_ship.update_index(global.selected_team[index]);
image_blend = color_for_element_background(obj_ship.element);
spr = object_get_sprite(global.ships[?global.selected_team[index]]);
